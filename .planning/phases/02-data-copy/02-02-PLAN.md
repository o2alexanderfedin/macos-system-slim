---
phase: 02-data-copy
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/phases/02-data-copy/copy-results.txt
autonomous: false
requirements: [MIGR-01, MIGR-02, MIGR-03]

must_haves:
  truths:
    - "Docker.raw exists at /Volumes/Unitek-B/Docker/Docker.raw"
    - "Copy logical size matches source logical size"
    - "Copy on-disk size matches source on-disk size (within 1% tolerance)"
    - "Original Docker.raw on internal disk is still present"
    - "Progress bar was visible during copy"
  artifacts:
    - path: "/Volumes/Unitek-B/Docker/Docker.raw"
      provides: "Copied Docker disk image"
    - path: ".planning/phases/02-data-copy/copy-results.txt"
      provides: "Captured output from copy execution"
      contains: "PASS"
  key_links:
    - from: "copy-docker.sh"
      to: "/Volumes/Unitek-B/Docker/Docker.raw"
      via: "cp -c or ditto copy command"
      pattern: "MIGR-01.*PASS"
    - from: "verify-copy.sh"
      to: "/Volumes/Unitek-B/Docker/Docker.raw"
      via: "stat and du size comparison"
      pattern: "MIGR-02.*PASS"
---

<objective>
Execute the Docker.raw copy and verify integrity.

Purpose: Run copy-docker.sh to perform the actual data migration, capture results, then independently verify with verify-copy.sh. This is the live execution step — it modifies the external volume.
Output: Docker.raw copied to external volume, copy-results.txt with PASS/FAIL output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-copy/02-CONTEXT.md
@.planning/phases/02-data-copy/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute copy-docker.sh and capture results</name>
  <files>.planning/phases/02-data-copy/copy-results.txt</files>
  <read_first>
    - .planning/phases/02-data-copy/02-01-SUMMARY.md (what scripts were created)
    - .planning/phases/02-data-copy/copy-docker.sh (the script being executed)
  </read_first>
  <action>
Run the copy script and capture output to copy-results.txt.

**Steps:**

1. Verify Docker is not running before executing:
   ```bash
   pgrep -f 'Docker' && echo "ABORT: Docker is running" && exit 1
   ```

2. Execute the copy script with output captured to both terminal and file:
   ```bash
   bash .planning/phases/02-data-copy/copy-docker.sh 2>&1 | tee .planning/phases/02-data-copy/copy-results.txt
   ```

3. Check exit code. If non-zero, the script already cleaned up partial files (per D-11, D-12). Report the failure.

4. After copy-docker.sh succeeds, run verify-copy.sh as independent confirmation:
   ```bash
   bash .planning/phases/02-data-copy/verify-copy.sh 2>&1 | tee -a .planning/phases/02-data-copy/copy-results.txt
   ```

**Expected duration:** 5-15 minutes depending on disk I/O speed for ~24 GB copy.

**If copy-docker.sh fails:** Do NOT retry manually. The script already has one built-in retry with ditto fallback. If both attempts fail, investigate the error in copy-results.txt before re-running.
  </action>
  <verify>
    <automated>test -f /Volumes/Unitek-B/Docker/Docker.raw && grep -c "PASS" .planning/phases/02-data-copy/copy-results.txt | xargs test 3 -le && echo "Copy verified"</automated>
  </verify>
  <acceptance_criteria>
    - test -f /Volumes/Unitek-B/Docker/Docker.raw (destination file exists)
    - grep -q 'MIGR-01.*PASS\|PASS.*MIGR-01' .planning/phases/02-data-copy/copy-results.txt (copy succeeded)
    - grep -q 'MIGR-02' .planning/phases/02-data-copy/copy-results.txt (size verification ran)
    - grep -c 'FAIL' .planning/phases/02-data-copy/copy-results.txt returns 0 (no failures)
  </acceptance_criteria>
  <done>Docker.raw exists at /Volumes/Unitek-B/Docker/Docker.raw, copy-results.txt contains at least 3 PASS lines and zero FAIL lines, verify-copy.sh independently confirmed integrity</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms copy results</name>
  <files>.planning/phases/02-data-copy/copy-results.txt</files>
  <read_first>
    - .planning/phases/02-data-copy/copy-results.txt (output from copy and verification)
  </read_first>
  <action>
Present copy results to user for confirmation. Display:
1. Contents of copy-results.txt showing all PASS/FAIL lines
2. Destination file listing: `ls -lh /Volumes/Unitek-B/Docker/Docker.raw`
3. Original file listing: `ls -lh ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw`
4. Ask user to confirm progress bar was visible during the copy (MIGR-03)
  </action>
  <what-built>Docker.raw has been copied from the internal disk to /Volumes/Unitek-B/Docker/Docker.raw. Both the copy script's built-in verification and the independent verify-copy.sh confirmed size integrity.</what-built>
  <how-to-verify>
    1. Review copy-results.txt: `cat .planning/phases/02-data-copy/copy-results.txt`
    2. Confirm all lines show [ PASS ] and none show [ FAIL ]
    3. Verify destination file exists: `ls -lh /Volumes/Unitek-B/Docker/Docker.raw`
    4. Verify original still exists: `ls -lh ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw`
    5. Confirm progress bar was visible during the copy (MIGR-03 — observed during execution)
  </how-to-verify>
  <verify>
    <automated>grep -c "PASS" .planning/phases/02-data-copy/copy-results.txt | xargs test 3 -le && echo "Sufficient PASS results"</automated>
  </verify>
  <acceptance_criteria>
    - User typed "approved"
    - copy-results.txt contains zero FAIL lines
  </acceptance_criteria>
  <done>User confirmed copy results are correct and progress was visible during transfer</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Independent verification after execution:
```bash
bash .planning/phases/02-data-copy/verify-copy.sh
```
Must exit 0 with all PASS results.
</verification>

<success_criteria>
- Docker.raw exists at /Volumes/Unitek-B/Docker/Docker.raw
- Logical file size matches source exactly
- On-disk size matches source (within 1% tolerance)
- Original Docker.raw on internal disk is unmodified
- copy-results.txt captured with all PASS, zero FAIL
- User confirmed results
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-copy/02-02-SUMMARY.md`
</output>
