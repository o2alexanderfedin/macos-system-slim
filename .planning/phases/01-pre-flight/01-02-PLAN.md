---
phase: 01-pre-flight
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-pre-flight/preflight-results.txt
autonomous: false
requirements:
  - PREFLT-01
  - PREFLT-02
  - PREFLT-03
  - PREFLT-04
  - PREFLT-05

must_haves:
  truths:
    - "Pre-flight script ran to completion with all checks passing"
    - "Docker Desktop is confirmed not running (process list clean)"
    - "External volume is APFS with sufficient free space"
    - "Both settings backup files exist at correct location"
    - "Target directory is writable"
    - "Apple Virtualization Framework backend confirmed"
    - "User has visually confirmed Docker is not in menubar"
  artifacts:
    - path: ".planning/phases/01-pre-flight/preflight-results.txt"
      provides: "Captured output from pre-flight execution"
      contains: "Safe to proceed to Phase 2"
    - path: "~/Library/Group Containers/group.com.docker/settings-store.json.bak"
      provides: "Backup of primary settings file"
    - path: "~/Library/Group Containers/group.com.docker/settings.json.bak"
      provides: "Backup of secondary settings file"
  key_links:
    - from: "preflight.sh"
      to: "preflight-results.txt"
      via: "stdout capture"
      pattern: "PASS.*PREFLT"
    - from: "verify-preflight.sh"
      to: "backup files"
      via: "test -f existence check"
      pattern: "settings-store.json.bak"
---

<objective>
Execute the pre-flight validation script and confirm all checks pass with user verification.

Purpose: This plan actually runs the pre-flight checks created in Plan 01 against the live system, captures results, then pauses for the user to visually confirm Docker is fully gone from the menubar. This is the gate for Phase 2.

Output: `preflight-results.txt` with all PASS lines, backup files created on disk, user confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight/01-CONTEXT.md
@.planning/phases/01-pre-flight/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute pre-flight script and capture results</name>
  <files>.planning/phases/01-pre-flight/preflight-results.txt</files>
  <read_first>
    - .planning/phases/01-pre-flight/preflight.sh (the script to execute)
    - .planning/phases/01-pre-flight/01-CONTEXT.md (D-08: hard stop on any failure)
  </read_first>
  <action>
Run the pre-flight script and capture output:

```bash
bash .planning/phases/01-pre-flight/preflight.sh 2>&1 | tee .planning/phases/01-pre-flight/preflight-results.txt
```

**If the script exits 0 (all checks passed):**
- The output file contains the full checklist with PASS/INFO lines
- Proceed to verification task

**If the script exits non-zero (any check failed):**
- The output file contains the FAIL line and ABORT message
- Per D-08: Do NOT proceed. Report the failure to the user with the exact FAIL/ABORT lines
- The user must fix the issue (e.g., stop Docker Desktop, mount volume) then re-run this plan

After successful execution, run the independent verification:
```bash
bash .planning/phases/01-pre-flight/verify-preflight.sh --full 2>&1 | tee -a .planning/phases/01-pre-flight/preflight-results.txt
```

Both scripts must exit 0 for this task to pass.
  </action>
  <verify>
    <automated>grep -c "PASS" .planning/phases/01-pre-flight/preflight-results.txt | xargs test 5 -le && grep "Safe to proceed to Phase 2" .planning/phases/01-pre-flight/preflight-results.txt</automated>
  </verify>
  <acceptance_criteria>
    - preflight-results.txt exists
    - preflight-results.txt contains "[ PASS ] PREFLT-01"
    - preflight-results.txt contains "[ PASS ] PREFLT-02"
    - preflight-results.txt contains "[ PASS ] PREFLT-03"
    - preflight-results.txt contains "[ PASS ] PREFLT-04"
    - preflight-results.txt contains "[ PASS ] PREFLT-05"
    - preflight-results.txt contains "Safe to proceed to Phase 2"
    - preflight-results.txt does NOT contain "[ FAIL ]"
    - File exists: ~/Library/Group Containers/group.com.docker/settings-store.json.bak
    - File exists: ~/Library/Group Containers/group.com.docker/settings.json.bak
  </acceptance_criteria>
  <done>Pre-flight script ran successfully with all 5 PREFLT checks passing, verification script confirmed results, backup files exist on disk, results captured to preflight-results.txt</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms Docker fully stopped and pre-flight results</name>
  <what-built>Pre-flight validation completed — Docker processes killed, volume verified APFS with sufficient space, settings backed up, target directory writable, Apple Virtualization Framework confirmed.</what-built>
  <how-to-verify>
    1. Check macOS menubar — Docker Desktop whale icon should NOT be present
    2. Review the pre-flight results output above — all lines should show [ PASS ]
    3. Confirm the INFO line about Apple Virtualization Framework says "confirmed"
    4. Confirm the diskSizeMiB resolution INFO line shows on-disk size (~24.1 GB)
    5. Open Finder and navigate to ~/Library/Group Containers/group.com.docker/ — confirm settings-store.json.bak and settings.json.bak exist
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Phase 2, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Pre-flight results file contains all PASS lines, no FAIL lines, and the "Safe to proceed" message. Backup files exist at the correct location. User has visually confirmed Docker is not running.
</verification>

<success_criteria>
- preflight-results.txt shows all 5 PREFLT checks as PASS
- No FAIL lines in output
- Both .bak backup files exist in ~/Library/Group Containers/group.com.docker/
- User approved the results (visual confirmation of Docker not in menubar)
- Phase 1 is complete — safe to proceed to Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight/01-02-SUMMARY.md`
</output>
