---
phase: 04-cleanup-and-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/phases/04-cleanup-and-hardening/cleanup-results.txt
  - .planning/phases/04-cleanup-and-hardening/harden-results.txt
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
must_haves:
  truths:
    - "Original Docker.raw is deleted from the internal disk"
    - "df output shows space reclaimed on internal disk"
    - "AutoDownloadUpdates is False in settings-store.json after Docker restart"
    - "DisableUpdate is True in settings-store.json after Docker restart"
    - "AutoStart is False in settings-store.json after Docker restart"
    - "Docker Desktop is running from external volume after hardening"
  artifacts:
    - path: ".planning/phases/04-cleanup-and-hardening/cleanup-results.txt"
      provides: "Captured output from cleanup.sh execution"
      contains: "PASS"
    - path: ".planning/phases/04-cleanup-and-hardening/harden-results.txt"
      provides: "Captured output from harden.sh execution"
      contains: "PASS"
  key_links:
    - from: ".planning/phases/04-cleanup-and-hardening/cleanup.sh"
      to: "$HOME/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw"
      via: "rm command deletes the file"
      pattern: "rm.*Docker\\.raw"
    - from: ".planning/phases/04-cleanup-and-hardening/harden.sh"
      to: "~/Library/Group Containers/group.com.docker/settings-store.json"
      via: "Python json module writes hardening keys"
      pattern: "AutoDownloadUpdates.*False"
---

<objective>
Execute cleanup.sh and harden.sh to complete the Docker data migration.

Purpose: This is the final execution step -- delete the original Docker.raw (~24 GB) from the internal disk and harden Docker Desktop settings against auto-update and login race conditions.

Output: cleanup-results.txt and harden-results.txt confirming all 4 CLEAN requirements satisfied, plus human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-and-hardening/04-CONTEXT.md
@.planning/phases/04-cleanup-and-hardening/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute cleanup.sh and harden.sh</name>
  <files>
    .planning/phases/04-cleanup-and-hardening/cleanup-results.txt
    .planning/phases/04-cleanup-and-hardening/harden-results.txt
  </files>
  <read_first>
    .planning/phases/04-cleanup-and-hardening/cleanup.sh
    .planning/phases/04-cleanup-and-hardening/harden.sh
    .planning/phases/04-cleanup-and-hardening/04-01-SUMMARY.md
  </read_first>
  <action>
Execute the two scripts in sequence. Each script tees output to its results file.

**Step 1: Run cleanup.sh**
```bash
bash .planning/phases/04-cleanup-and-hardening/cleanup.sh
```
Expected: verify-cutover.sh gate passes, Docker.raw deleted, df before/after captured, all PASS, exit 0.

If cleanup.sh fails:
- If verify-cutover.sh gate fails: Docker is not running correctly from external volume. Do NOT proceed. Report to user.
- If Docker.raw not found: It may already be deleted. Check and report.
- Any other failure: Report exact error, do not proceed to harden.sh.

**Step 2: Run harden.sh**
```bash
bash .planning/phases/04-cleanup-and-hardening/harden.sh
```
Expected: Docker stopped, settings updated with readback verify, Docker restarted, post-restart readback passes, migration summary printed, all PASS, exit 0.

If harden.sh fails:
- If Docker won't stop: Report to user for manual intervention.
- If Python settings update fails: Settings are unchanged, safe to investigate.
- If Docker won't restart: Check if external volume is mounted. Report diagnostics.
- If post-restart readback fails: Docker may have reverted settings on startup. Report exact key values.

**Step 3: Verify results files exist**
Confirm cleanup-results.txt and harden-results.txt both exist and contain PASS lines.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/temp && test -f .planning/phases/04-cleanup-and-hardening/cleanup-results.txt && test -f .planning/phases/04-cleanup-and-hardening/harden-results.txt && grep -c "PASS" .planning/phases/04-cleanup-and-hardening/cleanup-results.txt && grep -c "PASS" .planning/phases/04-cleanup-and-hardening/harden-results.txt</automated>
  </verify>
  <acceptance_criteria>
    - cleanup-results.txt exists and contains at least 3 PASS lines
    - cleanup-results.txt contains "CLEAN-01" and "CLEAN-02" in PASS lines
    - cleanup-results.txt contains df -h / output (BEFORE and AFTER)
    - harden-results.txt exists and contains at least 4 PASS lines
    - harden-results.txt contains "CLEAN-03" or "CLEAN-04" in PASS lines
    - harden-results.txt contains "Post-restart verification passed"
    - harden-results.txt contains "Migration Complete" summary
    - Docker.raw no longer exists at ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw
    - `docker info` succeeds (daemon running)
  </acceptance_criteria>
  <done>Both scripts exit 0, results files show all PASS, Docker.raw deleted from internal disk, Docker running from external volume with hardened settings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies migration is fully complete</name>
  <what-built>
    Phase 4 cleanup and hardening:
    1. Original Docker.raw deleted from internal disk (~24 GB reclaimed)
    2. Docker Desktop auto-update disabled (AutoDownloadUpdates=False, DisableUpdate=True)
    3. Docker Desktop start-at-login disabled (AutoStart=False)
    4. Docker is running from /Volumes/Unitek-B/Docker/ with all settings verified
  </what-built>
  <how-to-verify>
    1. Check internal disk space: Run `df -h /` -- should show ~24 GB more available than before cleanup
    2. Verify Docker.raw is gone: Run `ls ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw` -- should say "No such file"
    3. Verify Docker is running: Run `docker info` and `docker images` -- should show your images
    4. Check Docker Desktop Settings UI: Open Docker Desktop -> Settings -> General:
       - "Start Docker Desktop when you sign in" should be unchecked
       - Auto-update settings should show updates disabled
    5. (Optional) Verify .bak files still exist for rollback insurance:
       `ls ~/Library/Group\ Containers/group.com.docker/settings-store.json.bak`
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Post-execution verification:
1. `ls ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw 2>/dev/null && echo "EXISTS" || echo "DELETED"` -- must say DELETED
2. `docker info > /dev/null 2>&1 && echo "RUNNING" || echo "NOT RUNNING"` -- must say RUNNING
3. `python3 -c "import json,os; d=json.load(open(os.path.expanduser('~/Library/Group Containers/group.com.docker/settings-store.json'))); print('AutoDownloadUpdates:', d.get('AutoDownloadUpdates')); print('DisableUpdate:', d.get('DisableUpdate')); print('AutoStart:', d.get('AutoStart'))"` -- must show False, True, False
</verification>

<success_criteria>
- Original Docker.raw is deleted (CLEAN-01)
- df shows space reclaimed (CLEAN-02)
- AutoDownloadUpdates=False and DisableUpdate=True in settings (CLEAN-03)
- AutoStart=False in settings (CLEAN-04)
- Docker Desktop is running from external volume
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-and-hardening/04-02-SUMMARY.md`
</output>
