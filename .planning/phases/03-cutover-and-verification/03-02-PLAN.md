---
phase: 03-cutover-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-cutover-and-verification/cutover-results.txt
autonomous: false
requirements: [CONF-01, CONF-02, VERIF-01, VERIF-02, VERIF-03]

must_haves:
  truths:
    - "DataFolder in settings-store.json reads /Volumes/Unitek-B/Docker"
    - "Docker Desktop is running and docker info exits 0"
    - "At least one Docker image exists in docker images output"
    - "At least one container exists in docker ps -a output"
    - "docker run --rm hello-world completes successfully"
  artifacts:
    - path: ".planning/phases/03-cutover-and-verification/cutover-results.txt"
      provides: "Cutover execution results with pass/fail status"
      contains: "CONF-01"
  key_links:
    - from: "cutover.sh"
      to: "settings-store.json"
      via: "Python json update + readback verify"
      pattern: "DataFolder.*Unitek-B"
    - from: "verify-cutover.sh"
      to: "docker CLI"
      via: "docker images + docker ps -a + docker run"
      pattern: "VERIF-01.*PASS"
---

<objective>
Execute the cutover and verify Docker Desktop runs from the external volume.

Purpose: This is the live cutover -- updating Docker Desktop settings, starting Docker from the external volume, and confirming all pre-migration data survived. This is the critical phase gate: Phase 4 (cleanup/deletion of original) cannot proceed until this passes.
Output: cutover-results.txt with all checks passing, user confirmation of Docker running correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cutover-and-verification/03-CONTEXT.md
@.planning/phases/03-cutover-and-verification/03-RESEARCH.md
@.planning/phases/03-cutover-and-verification/03-01-SUMMARY.md

<interfaces>
<!-- Scripts created by Plan 01 -->

From .planning/phases/03-cutover-and-verification/cutover.sh:
- Updates DataFolder in settings-store.json and dataFolder in settings.json
- Starts Docker Desktop with `open -a "Docker Desktop"`
- Polls `docker info` every 5s with 120s timeout
- Emits diagnostics on failure (D-12)
- Writes results to cutover-results.txt
- Exit 0 = cutover succeeded, Exit 1 = cutover failed

From .planning/phases/03-cutover-and-verification/verify-cutover.sh:
- Checks DataFolder value in settings-store.json
- Counts images (docker images), expects > 0
- Counts containers (docker ps -a), expects > 0
- Runs hello-world smoke test
- Exit 0 = all checks pass, Exit 1 = verification failed
- Safe to re-run at any time (read-only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute cutover.sh</name>
  <files>.planning/phases/03-cutover-and-verification/cutover-results.txt</files>
  <read_first>
    - .planning/phases/03-cutover-and-verification/03-CONTEXT.md (decisions D-05 through D-13)
    - .planning/phases/03-cutover-and-verification/cutover.sh (the script being executed)
  </read_first>
  <action>
Run the cutover script to update Docker Desktop settings and start Docker from the external volume.

**Pre-execution checks (manual before running):**
1. Confirm Docker Desktop is NOT running: `pgrep -f 'Docker Desktop|com.docker.backend' || echo "No Docker processes"`
2. Confirm external volume is mounted: `test -d /Volumes/Unitek-B/Docker && echo "Volume mounted"`
3. Confirm Docker.raw copy exists: `test -f /Volumes/Unitek-B/Docker/Docker.raw && echo "Docker.raw present"`

**Execute:**
```bash
bash .planning/phases/03-cutover-and-verification/cutover.sh
```

**Expected outcome:**
- `[ PASS ] CONF-01: DataFolder updated to /Volumes/Unitek-B/Docker in both settings files`
- `[ PASS ] CONF-02: Docker daemon ready after Ns`
- cutover-results.txt created with passing results
- FAIL count = 0

**If cutover.sh fails:**
- Do NOT retry automatically (per D-11 -- investigate first)
- Read the diagnostic output emitted by the script
- Check cutover-results.txt for partial results
- The .bak files from Phase 1 exist as manual rollback insurance

**After successful cutover, run verification:**
```bash
bash .planning/phases/03-cutover-and-verification/verify-cutover.sh
```

**Expected verification outcome:**
- `[ PASS ] CONF-01: DataFolder = /Volumes/Unitek-B/Docker`
- `[ PASS ] VERIF-01: N image(s) present`
- `[ PASS ] VERIF-02: N container(s) present`
- `[ PASS ] VERIF-03: hello-world smoke test succeeded`
- FAIL count = 0

Save verification output by appending to cutover-results.txt:
```bash
echo "" >> .planning/phases/03-cutover-and-verification/cutover-results.txt
echo "=== Verification ===" >> .planning/phases/03-cutover-and-verification/cutover-results.txt
bash .planning/phases/03-cutover-and-verification/verify-cutover.sh | tee -a .planning/phases/03-cutover-and-verification/cutover-results.txt
```
  </action>
  <verify>
    <automated>grep -q 'CONF-01.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt && grep -q 'CONF-02.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt && echo "Cutover checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'CONF-01.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
    - grep -q 'CONF-02.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
    - grep -q 'VERIF-01.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
    - grep -q 'VERIF-02.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
    - grep -q 'VERIF-03.*PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
  </acceptance_criteria>
  <done>cutover-results.txt contains PASS for CONF-01, CONF-02, VERIF-01, VERIF-02, VERIF-03 with zero FAIL entries</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms Docker running from external volume</name>
  <read_first>
    - .planning/phases/03-cutover-and-verification/cutover-results.txt (execution results)
  </read_first>
  <what-built>Docker Desktop has been redirected to use /Volumes/Unitek-B/Docker as its data location. Settings files updated, Docker started, and automated verification passed (images present, containers present, hello-world ran successfully).</what-built>
  <how-to-verify>
    1. Check Docker Desktop is running: look for the whale icon in the macOS menubar
    2. Open a terminal and run: `docker images` -- confirm your expected images are listed
    3. Run: `docker ps -a` -- confirm your expected containers are listed (the broken Compose group containers appearing as stopped/error is expected and OK per D-09)
    4. Run: `docker run --rm hello-world` -- confirm "Hello from Docker!" output
    5. Check Activity Monitor or `du -sh ~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw` -- this file should still exist (Phase 4 deletes it) but Docker should NOT be using it
    6. If everything looks correct, type "approved"
    7. If any issues, describe what you see
  </how-to-verify>
  <acceptance_criteria>
    - User confirms Docker Desktop is running from external volume
    - User sees expected images and containers
    - User approves to proceed to Phase 4
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Cutover results file contains all passing checks:
```bash
grep -c 'PASS' .planning/phases/03-cutover-and-verification/cutover-results.txt
```
No FAIL entries in results:
```bash
grep -c 'FAIL' .planning/phases/03-cutover-and-verification/cutover-results.txt | grep -q '^0$' && echo "No failures"
```
</verification>

<success_criteria>
- cutover.sh executed successfully with 0 failures
- verify-cutover.sh executed successfully with 0 failures
- cutover-results.txt contains PASS for all 5 requirements (CONF-01, CONF-02, VERIF-01, VERIF-02, VERIF-03)
- User confirmed Docker Desktop is running correctly from external volume
</success_criteria>

<output>
After completion, create `.planning/phases/03-cutover-and-verification/03-02-SUMMARY.md`
</output>
